import React from "react";
import {
    Ranks,
    Suits,
} from "../components";

export default {
    title: 'Cards',
}

export const Suit_Symbols = () => (
    <Suits />
)

export const Rank_Symbols = () => (
    <Ranks />
)