import React from 'react';

console.log('env', process.env.REACT_APP_ENV)

export default {
    title: 'Graphics',
    component: SVG,
};

export const SVG = () => (
    <React.Fragment>
    </React.Fragment>
);
