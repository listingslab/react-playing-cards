import React from "react";
import {
    Card,
    Readme
} from "../Cards";

export default {
    title: 'Cards',
}

export const Readme_ = () => (<Readme />)
export const Pick_A_Card_ = () => (<Card />)
